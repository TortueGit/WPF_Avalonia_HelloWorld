using Avalonia;
using Avalonia.Controls;
using Avalonia.Markup.Xaml;

namespace MyApp.Views
{
    /// <summary>
    /// Xaml & related Class auto generated with command line 'dotnet new avalonia.usercontrol -o Views -n MyFirstNewListView --namespace MyApp.Views'
    /// </summary>
    public class MyFirstNewListView : UserControl
    {
        public MyFirstNewListView()
        {
            InitializeComponent();
        }

        private void InitializeComponent()
        {
            AvaloniaXamlLoader.Load(this);
        }
    }
}
